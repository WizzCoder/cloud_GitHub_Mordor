package JAXB;

public class TestWrite {

    public static void main(String[] args) {
        StaxWrite configFile = new StaxWriter();
        configFile.setFile("config2.xml");
        try {
            configFile.saveConfig();
        } catch (Exception e) {
            e.printStackTrace();
        }
    }
}
